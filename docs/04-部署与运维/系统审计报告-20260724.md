# 系统审计报告（2026-07-24）

## 结论

代码与本地数据库已升级至 `zfmd_pm 17.0.1.12.1`。已确认的高风险代码缺陷均已修复，生产部署基线已经补齐。当前状态适合进入预发布验收，但在完成账号治理、HTTPS 配置、异机备份和恢复演练之前，不应直接开放公网。

## 已修复

| 等级 | 问题 | 处理 |
|---|---|---|
| 严重 | 模块升级会自动创建固定口令 `zfmd` 账户 | 删除自动建号与固定口令逻辑；账号改由管理员显式创建 |
| 严重 | 导入映射模型绕过全部访问检查，并创建无分组全局权限 | 删除权限绕过和运行时 ACL；仅业务管理员与系统管理员可访问 |
| 高 | 备份、同步、预警重算及导入方法可被 RPC 直接调用 | 所有特权入口增加服务器端角色校验 |
| 高 | Excel 导出接受任意模型名称 | 增加 8 个业务模型白名单、读取权限和记录规则检查 |
| 高 | 备份下载读取服务器路径 | 校验文件必须位于当前数据库专用备份目录，增加禁止缓存响应头 |
| 高 | PostgreSQL 与 Odoo 端口绑定所有网卡 | 本地配置改为仅绑定 `127.0.0.1`；生产数据库不发布端口 |
| 高 | 部署脚本存在默认主密码/数据库密码，运行时配置权限过宽 | 生产模式拒绝默认口令，支持 secret 文件，临时配置权限设为 `0600` |
| 高 | 云端直接使用开发 Compose、浮动镜像和源码挂载 | 新增独立生产 Compose、固定镜像摘要、健康检查、重启策略和日志轮转 |
| 中 | 历史导入脚本硬编码 `admin/admin` | 改为必须通过环境变量传入登录密码 |
| 高 | PostgreSQL 18 客户端生成的备份含 PostgreSQL 16 不支持的设置 | 备份时移除不兼容设置，并通过严格恢复验证 |
| 中 | 数据库列表与代理姿态不适合公网 | 生产配置固定数据库与 `dbfilter`，关闭数据库列表并启用可信反向代理模式 |

## 验证结果

- Python 字节码编译：通过。
- 全部 XML 解析：通过。
- Shell 语法：通过。
- Docker Compose 生产配置解析：通过。
- Git 差异空白检查：通过。
- 生产镜像构建：通过，镜像内模块版本为 `17.0.1.12.1`。
- 全新数据库安装与自动化测试：21 个测试用例，0 失败、0 错误。
- 当前业务库模块升级：通过，版本为 `17.0.1.12.1`。
- 升级前后核心数量一致：合同 2,038、开票 2,350、回款 3,246、应收 4,351。
- 合同编号重复检查：0 条。
- HTTP 健康检查：Odoo 正常响应。
- 系统 ZIP：解包校验通过。
- Excel 业务备份：9 个工作表可读取。
- 隔离恢复演练：严格 SQL 恢复成功，核心记录数量一致，37 个附件缺失 0。

本次上线前恢复点：

- 系统 ZIP：`zfmd-PM-20260724-095131-667431.zip`
- SHA-256：`6bf621b6c3bc5a4385175d312f7644737c61a6b3625907bdd96580e135714bf1`
- Excel：`zfmd-PM-business-20260724-092803-993800.xlsx`
- SHA-256：`be6940a7153e3d97df6af7c0674ba85784075eb69ecbd2345f4f7d55a97c4261`

## 上线前仍需完成

1. 账号治理：当前本地库仍有 `admin`、`visitor` 等活动账号；确认用途后停用不需要的账号，并重置全部管理员与 `zfmd` 密码。服务器账号与本地账号不是同一身份，权限应分别核对。
2. 数据库降权：当前开发库的 `odoo` PostgreSQL 角色仍是超级用户；新生产配置会创建最小权限角色，迁移旧库后仍须执行降权确认。
3. 数据清理：有 1,071 条历史业务记录未关联到合同，其中 2 条回款记录连原始合同号也为空。其余未关联记录保留了原始编号，可在上线后分批匹配。
4. 灾备：现有备份仍在同一台机器。至少增加一份异机或对象存储副本，并设置失败与磁盘空间告警。
5. 公网入口：配置真实域名、HTTPS 证书、防火墙与 Nginx，确认数据库管理路由不可访问。
6. 人工 UI 验收：本次执行环境没有可用交互式浏览器；上线前必须人工验证登录、备份中心、下载、定时设置、导入、导出和移动/桌面布局。
7. 运维监控：配置存活、证书到期、磁盘、容器重启和备份失败告警。

## 发布判定

上述 1、2、4、5、6 项是正式公网切换的阻断项。全部完成并留下验收记录后，方可判定为可上线。
